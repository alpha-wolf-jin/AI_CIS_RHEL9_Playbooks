req_01: "Run the audit procedure for Ensure successful file system mounts are collected: On disk configuration"
req_02: "VERIFY: Summarise compliance status for Ensure successful file system mounts are collected. Expected result: PASS if compliant, FAIL otherwise"
req_03: "Store the final audit result in a variable named 'cis_result' using set_fact. Set cis_result to the overall compliance status (PASS/FAIL/SKIP/UNKNOWN) derived from the last requirement's status variable."
audit_rules_dir: "/etc/audit/rules.d"
expected_line_b64: "-a always,exit -F arch=b64 -S mount -F auid>=1000 -F auid!=unset -k mounts"
expected_line_b32: "-a always,exit -F arch=b32 -S mount -F auid>=1000 -F auid!=unset -k mounts"
