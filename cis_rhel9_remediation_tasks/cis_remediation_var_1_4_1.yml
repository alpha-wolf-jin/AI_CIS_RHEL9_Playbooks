playbook_name: "cis_1_4_1_bootloader_password"
backup_dir: "/var/log/ai-playbooks-backup/{{ playbook_name }}"
req_01: "Apply prerequisite check for grub2-setpassword using command: `which grub2-setpassword`. Expected result: path to grub2-setpassword binary."
req_02: "Apply bootloader password using command: `grub2-setpassword`. Expected result: password is set and encrypted."
req_03: "Verify bootloader password is set using command: `grep -q '^GRUB2_PASSWORD' /boot/grub2/user.cfg`. Expected result: exit code 0."
req_04: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
bootloader_password: "GrubP@ssw0rd!"
