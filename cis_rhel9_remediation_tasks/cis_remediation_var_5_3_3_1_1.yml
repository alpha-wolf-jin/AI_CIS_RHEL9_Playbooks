playbook_name: "cis_5_3_3_1_1"
checkpoint_id: "cis_5_3_3_1_1"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
faillock_conf: "/etc/security/faillock.conf"
deny_value: "5"
# Gating variable: set to true only for temporary testing; false for permanent remediation
restore_after_test: false
req_01: "Run the provided remediation script to fix Ensure password failed attempts lockout is configured"
req_02: "VERIFY: Run audit check to confirm Ensure password failed attempts lockout is configured is properly configured. Expected result: System is compliant"
req_03: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
req_04: "Generate remediation report"
