playbook_name: "cis_5_4_2_4"
checkpoint_id: "cis_5_4_2_4"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
req_01: "Apply a password for the root user using command: `passwd root`. Expected result: root password is set and root account is accessible via password authentication."
req_02: "OR lock the root user account using command: `usermod -L root`. Expected result: root account is locked and cannot be accessed directly."
req_03: "Verify the root account status using command: `passwd -S root`. Expected result: output shows either 'Password set' or 'L' (locked) depending on the chosen remediation."
req_04: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
root_password: "TempP@ssw0rd!2024"
