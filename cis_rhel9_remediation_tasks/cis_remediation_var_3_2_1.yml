kernel_module: "dccp"
module_type: "net"
checkpoint_id: "cis_3_2_1"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
script_dest: "/usr/local/bin/cis/cis_remediation_{{ checkpoint_id }}.sh"
req_01: "Run the provided remediation script to fix Ensure dccp kernel module is not available"
req_02: "VERIFY: Run audit check to confirm Ensure dccp kernel module is not available is properly configured"
req_03: "Generate remediation report"
