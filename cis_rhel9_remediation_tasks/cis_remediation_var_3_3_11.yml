checkpoint_id: "cis_3_3_11"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
sysctl_config_file: "/etc/sysctl.d/60-netipv6_sysctl.conf"
script_path: "{{ state_guard_dir }}/remediate_ra.sh"
req_01: "Run the provided remediation script to fix Ensure ipv6 router advertisements are not accepted - IF IPv6 enabled"
req_02: "VERIFY: Run audit check to confirm Ensure ipv6 router advertisements are not accepted is properly configured. Expected result: System is compliant"
req_03: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
