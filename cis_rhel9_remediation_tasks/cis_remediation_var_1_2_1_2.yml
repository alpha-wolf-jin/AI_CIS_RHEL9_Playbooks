playbook_name: "cis_1_2_1_2"
backup_dir: "/var/log/ai-playbooks-backup/{{ playbook_name }}"
req_01: "Check if dnf is installed using command: `rpm -q dnf`"
req_02: "Set gpgcheck=1 in /etc/dnf/dnf.conf"
req_03: "Set gpgcheck=1 in all .repo files under /etc/yum.repos.d/"
req_04: "Verify gpgcheck=1 globally"
req_05: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
