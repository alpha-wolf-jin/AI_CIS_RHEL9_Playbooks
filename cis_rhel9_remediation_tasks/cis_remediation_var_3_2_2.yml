checkpoint_id: "cis_3_2_2"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
kernel_module: "tipc"
script_path: "/var/local/cis_remediation_{{ kernel_module }}.sh"
# Requirement texts for reporting
req_01: "Run the provided remediation script to ensure tipc kernel module is not available"
req_02: "VERIFY: Run audit check to confirm Ensure tipc kernel module is not available is properly configured. Expected result: System is compliant"
req_03: "Generate remediation report"
