checkpoint_id: "cis_1_1_1_4"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
kernel_module: "hfsplus"
script_dest: "/tmp/remediate_{{ kernel_module }}.sh"
req_01: "1. Run the provided remediation script to fix Ensure hfsplus kernel module is not available."
req_02: "2. VERIFY: Run audit check to confirm Ensure hfsplus kernel module is not available is properly configured. Expected result: System is compliant"
