checkpoint_id: "cis_3_3_8"
state_guard_dir: "/usr/local/bin/cis/.cis_state_guard/{{ checkpoint_id }}"
state_guard_flag: "{{ state_guard_dir }}/checkpoint.flag"
sysctl_conf_dir: "/etc/sysctl.d"
ipv4_conf_file: "{{ sysctl_conf_dir }}/60-netipv4_sysctl.conf"
ipv6_conf_file: "{{ sysctl_conf_dir }}/60-netipv6_sysctl.conf"
ipv4_params:
  - "net.ipv4.conf.all.accept_source_route = 0"
  - "net.ipv4.conf.default.accept_source_route = 0"
ipv6_params:
  - "net.ipv6.conf.all.accept_source_route = 0"
  - "net.ipv6.conf.default.accept_source_route = 0"
req_01: "Run the provided remediation script to fix Ensure source routed packets are not accepted."
req_02: "VERIFY: Run audit check to confirm Ensure source routed packets are not accepted is properly configured. Expected result: System is compliant"
req_03: "Store the final remediation result in a variable named 'cis_result' using set_fact. Set cis_result to the overall remediation status (PASS/FAIL/SKIP/ERROR) derived from the last requirement's status variable."
req_04: "Generate remediation report"
